<h1>id: <% @task.id %>のタスク編集ページ</h1>

<%= form_for(@task) do |task_instance| %>
  <%= task_instance.label :content, 'タスク編集' %>
  <%= task_instance.text_field :content %>
  <%= task_instance.submit '編集' %>
<% end %>

<%= link_to '一覧に戻る', tasks_path %>

