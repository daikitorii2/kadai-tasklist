<h1>タスク一覧</h1>

<ul>
  <% @task.each do |task_instance| %>
    <li><%= link_to task_instance.id, task_instance %> : <%= task_instance.content %></li>
  <% end %>
</ul>

<%= link_to 'タスクを追加する', new_task_path %>